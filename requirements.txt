# MasriCX runtime dependencies.
# Training stack is intended for GPU environments (Kaggle/Linux).
# bitsandbytes is Linux-only; on Windows, 8-bit loading is unavailable.

PyYAML>=6.0
pydantic>=2,<3

# --- training stack (installed in the training environment, not in CI) ---
transformers>=4.44,<6.0
datasets>=2.20,<5.0
accelerate>=0.34,<2.0
peft>=0.12,<0.19
torch>=2.2,<3.0
bitsandbytes>=0.43; sys_platform == "linux"

# --- evaluation ---
jiwer>=3.0

# Required third-party baseline framework (EgypTalk-ASR-v2)
nemo_toolkit[asr]>=2.0,<3.0

# --- evaluation / audio audit (proven decoders used by the data audit) ---
librosa>=0.11,<1
soundfile>=0.12,<1

# --- dev/quality tools (also exposed via [project.optional-dependencies].dev) ---
ruff>=0.8,<1.0
mypy>=1.10
pytest>=8.0
pre-commit>=3.0
types-PyYAML
